# hospital_record
This project implements a Hospital Patient Records Management System using a Binary Search Tree (BST) data structure to efficiently store and manage patient information. Each patient record is represented as a node in the BST, organized by a unique patient ID to ensure fast and structured data access.
